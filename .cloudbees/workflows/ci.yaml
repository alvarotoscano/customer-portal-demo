apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: customer-portal

on:
  push:
    branches:
      - '**'

jobs:
  corporate-standard:
    uses: alvarotoscano/delivery-control-demo/.cloudbees/workflows/nodejs-standard.yaml@main